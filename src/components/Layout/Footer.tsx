import { ReactElement } from "react";
interface Props {}

function Footer({}: Props): ReactElement {
  return (
    <footer className="py-16 mx-2 sm:mx-10">
      <div></div>
    </footer>
  );
}

export default Footer;
