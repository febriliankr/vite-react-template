import React, { ReactElement } from "react";

interface Props {}

function about({}: Props): ReactElement {
  return (
    <div>
      <h1 className="">OMG TAILWIND</h1>
    </div>
  );
}

export default about;
