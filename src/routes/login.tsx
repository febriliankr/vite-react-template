import React, { ReactElement } from "react";

interface Props {}

function LoginPage({}: Props): ReactElement {
  return (
    <div>
      <h1>Login page</h1>
    </div>
  );
}

export default LoginPage;
